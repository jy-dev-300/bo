from evaluation.models import RAGCaseResult, RAGGoldCase
from rag.models import GroundedAnswer


def evaluate_rag_case(gold: RAGGoldCase, answer: GroundedAnswer) -> RAGCaseResult:
    """Student Implementation F: evaluate four dimensions without collapsing them."""
    raise NotImplementedError("Student Implementation F: separated RAG evaluation")

